spring:
  application:
    name: self-healing
  cloud:
    kubernetes:
      config:
        enabled: true
        sources:
        - namespace: default
          name: self-healing
      reload:
        enabled: true
        mode: event
        strategy: refresh